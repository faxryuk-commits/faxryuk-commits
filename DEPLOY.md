# Инструкция по развертыванию в облаке

## 🚀 Подготовка

### 1. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен (он понадобится при развертывании)

### 2. Локальная настройка

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

## ☁️ Развертывание на Railway

[Railway](https://railway.app) - простой способ развернуть бота бесплатно.

### Шаги:

1. **Регистрация**: Перейдите на [railway.app](https://railway.app) и войдите через GitHub

2. **Создание проекта**:
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройка переменных окружения**:
   - Перейдите в Settings → Variables
   - Добавьте переменную: `TELEGRAM_BOT_TOKEN` = ваш токен

4. **Развертывание**:
   - Railway автоматически определит Dockerfile
   - Бот запустится автоматически

5. **Проверка**:
   - Перейдите в Logs для просмотра логов
   - Найдите в Telegram вашего бота и отправьте `/start`

## 🌐 Развертывание на Heroku

### Шаги:

1. **Установка Heroku CLI**: [devcenter.heroku.com/articles/heroku-cli](https://devcenter.heroku.com/articles/heroku-cli)

2. **Логин**:
```bash
heroku login
```

3. **Создание приложения**:
```bash
heroku create ваш-bot-имя
```

4. **Установка переменных окружения**:
```bash
heroku config:set TELEGRAM_BOT_TOKEN=your_bot_token_here
```

5. **Развертывание**:
```bash
git push heroku main
```

6. **Проверка логов**:
```bash
heroku logs --tail
```

## 🐳 Развертывание через Docker

### Локальный запуск:

```bash
# Сборка образа
docker build -t parser-bot .

# Запуск контейнера
docker run -d \
  --name parser-bot \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  parser-bot
```

### Docker Compose:

```bash
docker-compose up -d
```

## 📦 Развертывание на VPS (Ubuntu/Debian)

### 1. Установка зависимостей:

```bash
sudo apt update
sudo apt install -y python3-pip python3-venv git
```

### 2. Клонирование проекта:

```bash
git clone <your-repo-url>
cd AI
```

### 3. Установка зависимостей:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 4. Создание .env файла:

```bash
nano .env
# Добавьте: TELEGRAM_BOT_TOKEN=your_token
```

### 5. Запуск через systemd:

Создайте файл `/etc/systemd/system/parser-bot.service`:

```ini
[Unit]
Description=Parser Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/AI
Environment="PATH=/path/to/AI/venv/bin"
ExecStart=/path/to/AI/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable parser-bot
sudo systemctl start parser-bot
sudo systemctl status parser-bot
```

## 🔧 Проверка работы

После развертывания:

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Попробуйте команды:
   - `/wb ноутбук` - парсинг Wildberries
   - `/yandex ресторан Москва` - парсинг Яндекс.Карт
   - `/stats` - статистика данных

## ⚠️ Важные замечания

1. **Бесплатные платформы** могут ограничивать ресурсы
2. **Railway** дает 500 часов бесплатного времени в месяц
3. **Heroku** требует кредитную карту для верификации
4. Для **постоянной работы** лучше использовать VPS или Railway

## 📝 Мониторинг

Для мониторинга работы бота используйте:
- Логи платформы (Railway/Heroku)
- Команду `/stats` в боте
- Файлы в директории `data/`

## 🔄 Обновление бота

После изменений в коде:

**Railway**: Автоматически перезапускается при push в GitHub

**Heroku**:
```bash
git push heroku main
```

**VPS**:
```bash
git pull
sudo systemctl restart parser-bot
```

**Docker**:
```bash
docker-compose down
docker-compose up -d --build
```
