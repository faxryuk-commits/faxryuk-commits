# 📤 Инструкция по отправке в GitHub

## Быстрый способ:

### 1. Создайте репозиторий на GitHub:

1. Перейдите на [github.com](https://github.com)
2. Нажмите кнопку **"+"** → **"New repository"**
3. Введите название (например: `parser-bot`)
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите **"Create repository"**

### 2. Подключите репозиторий:

**Автоматически:**
```bash
./setup_github.sh
```
Введите ваш GitHub username и название репозитория.

**Вручную:**
```bash
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
```

### 3. Отправьте код:

**Автоматически:**
```bash
./push_to_github.sh
```

**Вручную:**
```bash
git push -u origin main
```

---

## Альтернативный способ (HTTPS с токеном):

Если нужна аутентификация:

1. Создайте Personal Access Token:
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token
   - Выберите scope: `repo`

2. Используйте токен вместо пароля:
```bash
git remote add origin https://YOUR_TOKEN@github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

---

## После успешного push:

✅ Репозиторий будет доступен на GitHub  
✅ Можно подключить к Railway для автоматического деплоя  
✅ Можно клонировать на другие машины  

---

## Полезные команды:

```bash
# Проверить remote
git remote -v

# Изменить remote URL
git remote set-url origin https://github.com/NEW_USERNAME/NEW_REPO.git

# Посмотреть статус
git status

# Посмотреть историю коммитов
git log --oneline
```
