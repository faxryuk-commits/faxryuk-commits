# Парсер маркетплейсов и данных организаций с карт

Универсальный парсер для сбора данных с:
- **Маркетплейсов**: Wildberries, Ozon, AliExpress и др.
- **Карт**: Google Maps, Яндекс.Карты, 2ГИС

## 🤖 Telegram Бот

Проект включает Telegram бота для удобного управления парсером через команды.

### Команды бота:

- `/start` - Начать работу с ботом
- `/help` - Список всех команд

**Маркетплейсы:**
- `/wb <запрос>` - Поиск на Wildberries
- `/ozon <запрос>` - Поиск на Ozon

**Карты:**
- `/yandex <запрос> [город]` - Поиск в Яндекс.Картах
- `/google <запрос> [город]` - Поиск в Google Maps
- `/2gis <запрос> [город]` - Поиск в 2ГИС

**Управление:**
- `/stats` - Статистика сохраненных данных
- `/clear` - Очистить данные

## 🚀 Быстрый старт

### Локальная установка:

```bash
# Клонирование репозитория
git clone <your-repo-url>
cd AI

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
cp .env.example .env
# Отредактируйте .env и добавьте TELEGRAM_BOT_TOKEN

# Запуск бота
python main.py
```

### Развертывание в облаке:

Подробная инструкция в [DEPLOY.md](DEPLOY.md)

**Быстрый деплой на Railway:**
1. Создайте аккаунт на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Добавьте переменную `TELEGRAM_BOT_TOKEN` в настройках
4. Бот запустится автоматически

## Архитектура

Проект использует модульную архитектуру:
- Отдельные парсеры для каждого источника данных
- Единый интерфейс для всех парсеров
- Асинхронная обработка данных
- Конфигурируемые задержки и прокси
- Telegram бот для управления

## Структура проекта

```
├── bot/                    # Telegram бот
│   └── telegram_bot.py    # Основной файл бота
├── parsers/
│   ├── marketplace/       # Парсеры маркетплейсов
│   ├── maps/              # Парсеры карт
│   └── base.py            # Базовый класс парсера
├── models/                # Модели данных
├── storage/               # Хранение данных
├── main.py                # Точка входа (запуск бота)
├── example_usage.py       # Примеры использования парсеров
├── Dockerfile             # Docker конфигурация
├── docker-compose.yml     # Docker Compose
└── DEPLOY.md              # Инструкция по развертыванию
```

## Использование

### Через Telegram бота:

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Используйте команды для парсинга

**Примеры:**
```
/wb ноутбук
/yandex ресторан Москва
/google кафе Санкт-Петербург
/2gis аптека Москва
/stats
```

### Программно:

```python
from parsers.marketplace import WildberriesParser
from parsers.maps import YandexMapsParser

# Парсинг маркетплейса
wb_parser = WildberriesParser()
products = wb_parser.parse_search("ноутбук")

# Парсинг организаций
maps_parser = YandexMapsParser()
organizations = maps_parser.search_organizations("ресторан", "Москва")
```

## Важно

⚠️ Соблюдайте правила использования сайтов и rate limits
⚠️ Используйте задержки между запросами
⚠️ Учитывайте юридические аспекты парсинга
⚠️ Не злоупотребляйте парсингом - это может привести к блокировке IP

## Лицензия

MIT
